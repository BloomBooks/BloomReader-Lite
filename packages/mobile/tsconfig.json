{
    "extends": "expo/tsconfig.base",
    "compilerOptions": {
        "strict": true,
        "paths": {
            // The VSCode Intellisense works better this way
            "@shared/*": ["../shared/dist/*"]
            // More proper long-term I believe, but intellisense doesn't work as well
            // "@shared/*": ["./node_modules/bloom-reader-lite-shared/dist/*"]
        }
    }
}
